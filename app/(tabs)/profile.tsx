import { Placeholder } from "@/src/components/ui/Placeholder";

export default function ProfileTab() {
  return (
    <Placeholder
      title="Profile"
      subtitle="Placeholder — Phase 18."
      links={[{ label: "Log out (to splash)", href: "/" }]}
    />
  );
}

